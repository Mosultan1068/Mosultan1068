create MATERIALIZED view emp_dept as 
	select e.emp_id, 
       e.first_name, 
	   e.last_name, 
	   e.dept_id,
	   d.dept_name, 
	   d.location
	from employee e
	inner join department d on e.dept_id = d.dept_id
	order by d.location
with data;